[package]
name = "faf-units"
version = "0.1.0"
edition = "2021"
authors = ["octopus contributors"]
description = "Shared types and computation helpers for Forged Alliance Forever (FAF) units."
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
