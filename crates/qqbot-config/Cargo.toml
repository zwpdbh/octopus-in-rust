[package]
name = "qqbot-config"
version = "0.1.0"
edition = "2021"
authors = ["qqbot contributors"]
description = "Shared configuration types for qqbot-core and the qqbot CLI."
license = "MIT"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
