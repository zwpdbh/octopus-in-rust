[package]
name = "faf-ml-core"
version = "0.1.0"
edition = "2021"
authors = ["octopus contributors"]
description = "Shared serde types for the faf-ml data platform (server + web)."
license = "MIT"

[dependencies]
serde = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true, features = ["serde"] }
