[package]
name = "faf-quantities"
version = "0.1.0"
edition = "2021"
authors = ["octopus contributors"]
description = "Strongly-typed resource quantities for FAF build-order simulation."
license = "MIT"

[dependencies]
serde = { workspace = true }
