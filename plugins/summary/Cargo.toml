[package]
name = "summary"
version = "0.1.0"
edition = "2021"
authors = ["qqbot contributors"]
description = "Default qqbot plugin: conversation summarizer."
license = "MIT"

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]
test = false
